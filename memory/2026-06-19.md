# 2026-06-19 日志

## 改动

### gts-e2e-auto skill 更新
- Step 1 分情况 A/B：明确指定操作/验收→直接执行；没指定→自动分析改动出方案待确认
- 等待兄弟确认时发飞书通知

### gts-dev-workflow skill 更新
- 新增集成测试要求（`test/integration/`，Jest 跑，模拟真实操作链路）
- 更新笔记从「看情况」改为「强制必做」

### gts-recall skill 新建
- 回忆技能：查记忆 + 查近对话 → 给出下一步建议

### Plugin hook 实验（失败）
- `token-burst-guard`：token 使用限制插件。阈值太低把自己卡死，已删除。
- `ok-to-go`：「没说开干不准开干」插件。external plugin 的 `api.on` 注册始终不生效（hookCount=0）。linked .ts/.js 均无效。已放弃。
- `openclaw config set --json` 命令损坏了 `openclaw.json`（写成了"test"）。兄弟手动修复。

## 教训
- External plugin hook 在当前 OpenClaw 版本不可靠
- `config set` 命令要谨慎使用
