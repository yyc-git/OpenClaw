
### 第5轮（最终）：使用自定义 Sprite 类 ✅
**现象:** 蓝色 Mesh 可见，所有 Sprite（血条+红绿黄诊断）不可见
**诊断:** `calls` 差 2 —— Sprite 不产生 draw call，Three.js SpritePlugin 渲染空列表
**根因:** THREE.Sprite 共享 `_geometry`（InterleavedBuffer + InterleavedBufferAttribute）的 `_glAttribute`/`_glBuffer` 残留旧 WebGL context 的 buffer ID。新 renderer 检查 `attribute._glAttribute` 发现已存在（指向旧 context），不复传到新 context → Sprite 无 draw call
**修复:** 使用 `meta3d-jiehuo-abstract/src/three/Sprite.js` 自定义 Sprite：
- `dispose()` 把共享 `_geometry = undefined`
- `clearAllSprites` 中调用 `sprite.dispose()`
- `ThreeRenderer.dispose()` 加 `forceContextLoss()` + program/info 清理
**验证:** 双局血条和诊断 Sprite 都正确渲染 ✅
**决策记录:** `笔记/决策记录/2026-06-19-场景重入HUD不显示-Sprite共享几何体残留.md`
### 第5轮（最终）：使用自定义 Sprite 类 ✅
**现象:** 蓝色 Mesh 可见，所有 Sprite（血条+红绿黄诊断）不可见
**诊断:** `calls` 差 2 —— Sprite 不产生 draw call，Three.js SpritePlugin 渲染空列表
**根因:** THREE.Sprite 共享 `_geometry`（InterleavedBuffer + InterleavedBufferAttribute）的 `_glAttribute`/`_glBuffer` 残留旧 WebGL context 的 buffer ID。新 renderer 检查 `attribute._glAttribute` 发现已存在（指向旧 context），不复传到新 context → Sprite 无 draw call
**修复:** 使用 `meta3d-jiehuo-abstract/src/three/Sprite.js` 自定义 Sprite：
- `dispose()` 把共享 `_geometry = undefined`
- `clearAllSprites` 中调用 `sprite.dispose()`
- `ThreeRenderer.dispose()` 加 `forceContextLoss()` + program/info 清理
**验证:** 双局血条和诊断 Sprite 都正确渲染 ✅
**决策记录:** `笔记/决策记录/2026-06-19-场景重入HUD不显示-Sprite共享几何体残留.md`

---

### 强制重启丢失 commit 恢复（2026-06-19 11:30）
**事件:** 强制重启电脑后，`92c10cf3c` 和 `bd07b7a75` 两个 commit 从 GTS-Play `dev` 分支历史脱落
**恢复:** 从 `92c10cf3c` reflog 中 git checkout 捞回 5 个笔记文件 + `doc/v2.0-alpha.12.org`
**代码部分:** 无需恢复（已在工作区，包含在后续 commit 中）

### gts-code-review 新增两条规则（2026-06-19 11:37）
通过 skill_workshop 更新：
- 🟡 未使用项目自定义 Three.js class — 应改用 `meta3d-jiehuo-abstract/src/three/` 中的修改版
- 🟡 存在 setTimeout/异步过多 — 替换为 deltaTime/rAF 机制
- 🟢 异步调用无超时/错误处理

### setTimeout → addDeferTask·Flow 风格重构（2026-06-19 11:45-12:14）
**触发:** 代码审核 MultiplayerManager.ts 发现 `setTimeout(() => Modal.alert(...), 100)`
**方案演化:**
- ❌ 模块级 `let _tasks` → 兄弟要求移到 state
- ✅ `deferTasks` 字段在 `state.multiplayer.deferTasks`
- ❌ 帧循环中用本地 performance.now deltaTime → 兄弟指出 deltaTime 必须来自服务端
- ✅ processDeferTasks 在 listenGameState 回调末尾用 `msg.deltaTime` 驱动
- JEST guard 移除（addDeferTask 只存不执行，测试中安全）

### BDD 测试基础设施修复（2026-06-19 12:00-12:08）
- 新建 `tsconfig.test.json`（include 加 test）
- ts-jest 必须用 `globals['ts-jest']['tsconfig']` 格式，inline 不生效
- 子目录搬迁路径修复：unit/+integration/.steps.ts 的路径加一级 `../`

### 代码审核进度（2026-06-19）
- ✅ MultiplayerManager.ts — 完成
- ✅ MultiplayerLoop.ts — 完成
- ⏳ ThreeRenderer.ts / MultiplayerHUD.ts / MultiplayerRender.ts / Game.ts (room-service) — 待审
### 第5轮（最终）：使用自定义 Sprite 类 ✅
**现象:** 蓝色 Mesh 可见，所有 Sprite（血条+红绿黄诊断）不可见
**诊断:** `calls` 差 2 —— Sprite 不产生 draw call，Three.js SpritePlugin 渲染空列表
**根因:** THREE.Sprite 共享 `_geometry`（InterleavedBuffer + InterleavedBufferAttribute）的 `_glAttribute`/`_glBuffer` 残留旧 WebGL context 的 buffer ID。新 renderer 检查 `attribute._glAttribute` 发现已存在（指向旧 context），不复传到新 context → Sprite 无 draw call
**修复:** 使用 `meta3d-jiehuo-abstract/src/three/Sprite.js` 自定义 Sprite：
- `dispose()` 把共享 `_geometry = undefined`
- `clearAllSprites` 中调用 `sprite.dispose()`
- `ThreeRenderer.dispose()` 加 `forceContextLoss()` + program/info 清理
**验证:** 双局血条和诊断 Sprite 都正确渲染 ✅
**决策记录:** `笔记/决策记录/2026-06-19-场景重入HUD不显示-Sprite共享几何体残留.md`
### 第5轮（最终）：使用自定义 Sprite 类 ✅
**现象:** 蓝色 Mesh 可见，所有 Sprite（血条+红绿黄诊断）不可见
**诊断:** `calls` 差 2 —— Sprite 不产生 draw call，Three.js SpritePlugin 渲染空列表
**根因:** THREE.Sprite 共享 `_geometry`（InterleavedBuffer + InterleavedBufferAttribute）的 `_glAttribute`/`_glBuffer` 残留旧 WebGL context 的 buffer ID。新 renderer 检查 `attribute._glAttribute` 发现已存在（指向旧 context），不复传到新 context → Sprite 无 draw call
**修复:** 使用 `meta3d-jiehuo-abstract/src/three/Sprite.js` 自定义 Sprite：
- `dispose()` 把共享 `_geometry = undefined`
- `clearAllSprites` 中调用 `sprite.dispose()`
- `ThreeRenderer.dispose()` 加 `forceContextLoss()` + program/info 清理
**验证:** 双局血条和诊断 Sprite 都正确渲染 ✅
**决策记录:** `笔记/决策记录/2026-06-19-场景重入HUD不显示-Sprite共享几何体残留.md`

---

### 强制重启丢失 commit 恢复（2026-06-19 11:30）
**事件:** 强制重启电脑后，`92c10cf3c` 和 `bd07b7a75` 两个 commit 从 GTS-Play `dev` 分支历史脱落
**恢复:** 从 `92c10cf3c` reflog 中 git checkout 捞回 5 个笔记文件 + `doc/v2.0-alpha.12.org`
**代码部分:** 无需恢复（已在工作区，包含在后续 commit 中）

### gts-code-review 新增两条规则（2026-06-19 11:37）
通过 skill_workshop 更新：
- 🟡 未使用项目自定义 Three.js class — 应改用 `meta3d-jiehuo-abstract/src/three/` 中的修改版
- 🟡 存在 setTimeout/异步过多 — 替换为 deltaTime/rAF 机制
- 🟢 异步调用无超时/错误处理

### setTimeout → addDeferTask·Flow 风格重构（2026-06-19 11:45-12:14）
**触发:** 代码审核 MultiplayerManager.ts 发现 `setTimeout(() => Modal.alert(...), 100)`
**方案演化:**
- ❌ 模块级 `let _tasks` → 兄弟要求移到 state
- ✅ `deferTasks` 字段在 `state.multiplayer.deferTasks`
- ❌ 帧循环中用本地 performance.now deltaTime → 兄弟指出 deltaTime 必须来自服务端
- ✅ processDeferTasks 在 listenGameState 回调末尾用 `msg.deltaTime` 驱动
- JEST guard 移除（addDeferTask 只存不执行，测试中安全）

### BDD 测试基础设施修复（2026-06-19 12:00-12:08）
- 新建 `tsconfig.test.json`（include 加 test）
- ts-jest 必须用 `globals['ts-jest']['tsconfig']` 格式，inline 不生效
- 子目录搬迁路径修复：unit/+integration/.steps.ts 的路径加一级 `../`

### 代码审核进度（2026-06-19）
- ✅ MultiplayerManager.ts — 完成
- ✅ MultiplayerLoop.ts — 完成
- ⏳ ThreeRenderer.ts / MultiplayerHUD.ts / MultiplayerRender.ts / Game.ts (room-service) — 待审

---

### ThreeRenderer 拆文件 → DebugHelper（2026-06-19 14:40-14:47）
**原因:** ThreeRenderer.ts ~700 行，OBB 调试/销毁逻辑可拆出
**做法:**
1. 新建 `ThreeRendererDebug.ts`（约 275 行），存放 OBB 线框、碰撞盒调试、场景重入清理
2. ThreeRenderer.ts 委托给 debug 实例（约 425 行）
3. 应兄弟要求重命名为 `DebugHelper.ts`（git 识别为重命名，97% 相似度）
**commit:** `04834c5bf`

### 模块级变量 → state 迁移完成（2026-06-19 14:35）
**迁移清单:**
| 变量 | 原位置 | 新位置 |
|---|---|---|
| `_mpRafId` | 模块级 `let` | `state.multiplayer.mpRafId` |
| `_mpFrameCount` | 模块级 `let` | `state.multiplayer.mpFrameCount` |
| `_mpFrameSkip` | 模块级 `let` | `state.multiplayer.mpFrameSkip` |
| `_obbPartNames` | 模块级 `let Map` | `state.multiplayer.obbPartNames` |
| `_localHullOBB` | 模块级 `let` | `state.multiplayer.localHullOBB` |
| `_remoteHullCache` | 模块级 `let Map` | `state.multiplayer.remoteHullCache` |

### SKILL.md 同步 + MEMORY.md 清理重复（2026-06-19 14:36-14:38）
- SKILL.md 🟡 新增: 「禁止 window 全局挂载」「模块级变量未移入 state」
- SKILL.md 🟢 更新: 「未清理的全局对象」
- SKILL.md Step 2 增加兄弟回复规则说明
- MEMORY.md 删除「代码审核重构规则」重复小节（SKILL.md 是唯一权威）
- 回复规则移到工作协议中

### any 类型修复（2026-06-19 14:35-14:36）
**触发:** Step 2 确认时兄弟指出「没写内容=直接重构」
- `ImmMap<string, any>()` → `ImmMap<string, playerState>()`
- `obbArray: any[]` → `ReturnType<typeof computeOBBsFromAssets>`

### BDD 全绿修复（2026-06-19 14:48-14:52）
**现象:** 12 个测试因 `Sprite_1.Sprite is not a constructor` 失败
**根因:** `meta3d-jiehuo-three.js` mock 缺少 `Sprite` 导出；MockSprite 的 `scale.set` 不设属性
**修复:** 补 `MockSprite` class + 使用 `MockVec3`（set 实际写 x/y/z）
**结果:** 115 passed, 115 total ✅
**commit:** `78020c275`

### 审核继续至 MultiplayerRender.ts + MultiplayerHUD.ts（2026-06-19 14:52-14:54）
**🟡 待确认（3 处）:**
1. MultiplayerRender.ts — 模块级 `let` 变量（`_renderer`, `_mmdCharacters`, `_prevAnimations`）
2. MultiplayerHUD.ts — `_textureCache` 模块级 `let Map`
3. MultiplayerHUD.ts — `any` 类型（`damageSprites.forEach((data: any, ...)`）
**状态:** 兄弟尚未回复，等待确认是否开工


---

### state 入参化完成 — MultiplayerRender.ts 全部通过（2026-06-19 14:54-15:38）
**改动:** MultiplayerRender.ts 中所有基础设施函数接受 `mp` 首参，不隐式调 `readState()`/`writeState()`
**涉及函数:** `setPosition`, `addEntity`, `createMixer`, `createAction`, `playAction`, `setRotationY`, `setCollisionColor`, `crossFade`, `updateMixers`, `followTarget`, `initOrbitControls`, `render`, `loadFbxModelFromBuffer`, `removeEntity` 以及所有 MMD 函数
**兄弟要求额外改动:** `_getRenderer(mp?)` 的 `mp` 可选参数改为**必传**，不做 fallback。`init()`/`dispose()` 保留 `readState`/`writeState`（生命周期入口例外）
**MMD 函数:** `createMMDCharacter`, `removeMMDCharacter`, `setMMDTransform`, `setMMDAnimation`, `updateMMDAnimations`, `getOBBAssets`, `getAnimationDuration` 全部接收 `mp` 参数
**测试:** BDD 115/115 全部通过 ✅
**commit:** `72678842c` 「refactor: MultiplayerRender 所有函数接受 mp 参数（去 readState）」

### 审核完成文件（2026-06-19 14:54-15:38）
- ✅ MultiplayerManager.ts — setTimeout→addDeferTask, 回调剥离, 消息类型化
- ✅ MultiplayerLoop.ts — window 全局清理, 模块变量→state, any 类型修复
- ✅ ThreeRenderer.ts → DebugHelper.ts — 拆文件, OBB 调试剥离
- ✅ MultiplayerRender.ts — 全部函数接受 mp 必传参数
- ✅ MultiplayerHUD.ts — any 类型修复, _textureCache→state
- ⏳ Game.ts (room-service), Movement.res, serviceProto.ts — **待继续审核**

### 回复规则确认（2026-06-19 14:36）
- 代码审核 Step 2 兄弟回复规则：**没写内容=直接重构**；写了内容=按内容改；只有「不处理」「跳过」「忽略」明确词才不处理
- 此规则已写入 SKILL.md，MEMORY.md 保留说明

### gts-code-review SKILL.md 更新 — 飞书通知规则（2026-06-19 15:38）
- 三个 ⏸️ 停等确认点都加 📣 **飞书通知**：
  - Step 1 扫描完 → 通知改动清单
  - Step 2 出方案 → 通知 🟡 重构项等确认
  - Step 4 出摘要 → 通知摘要等确认
- 描述缩短为：「全手动交互：扫描→审查→确认后重构→测试→摘要。等确认时发飞书通知。」


---

### CubeMove → PlayerMove 重命名（2026-06-19 15:59-16:02）
**兄弟要求:** 去掉 Cube 前缀，改为 PlayerMove / PlayerNotMove
**范围（全局替换）:**
| 层 | 文件 | 改动 |
|---|---|---|
| TypeScript 类型 | `CommandType.ts` | 枚举值 |
| ReScript 类型 | `CommandType.res` ×3 | 字典 key |
| ReScript 逻辑 | `Movement.res` ×3 | 函数名 + 注释 |
| 前端命令 | `CommandManager.ts` | 属性访问 |
| BDD 测试 | steps + feature ×4 | 全部同步 |
**测试:** frontend 115/115 ✅ + room 19/19 ✅
**commit:** 包含在 `afb5fd1ad`

### SKILL.md 新增重命名规则（2026-06-19 16:08-16:12）
**触发:** 兄弟要求补全「名字需要重命名」重构规则
**6 种场景:** ① 名字未能清晰表达意图 ② 名字与实现不一致（谎言式命名）③ 业务术语不统一 ④ 过度宽泛 ⑤ 隐喻/模式混淆 ⑥ 语言/框架约定变更
**操作纪律:** 先改名再改逻辑 → 改名后确保测试通过 → 出方案列建议名，确认后改
**额外:**
- 🟡 新增「测试 mock 过多 → 重构生产函数」（按兄弟选择「思路2：高层编排测试用集成测，单元测只测纯逻辑」）
- 🟡 新增「测试代码不省略」回退（Step 1 扫到测试文件改动也必须查看）
- 改正 Step 1 描述：「不筛 test/，按 diff 的完整范围来」

### 共享 Mock 提取（2026-06-19 16:13-16:22）
**原因:** 测试代码手动堆砌 jest.fn() 和 mock 对象，多文件重复
**结果:**
| 文件 | 内容 |
|---|---|
| `test/helpers/mockThreeRenderer.ts` | `createMockThreeRenderer()` + `createMockCamera()` |
| `test/helpers/testUtils.ts` | `mkPlayer()` + `parsePlayerTable()` |
**已改造的测试文件:**
- `multiplayer-hud.steps.ts` — 改用共享 `createMockThreeRenderer`
- `multiplayer-render.steps.ts` — 改用共享 `mkPlayer`/`parsePlayerTable`
- `reentry-hud.steps.ts` — 保持原样（自己独特的 mock）

### 本次代码审核终态（2026-06-19 16:22）
| 类别 | 项 | 状态 |
|---|---|---|
| 🔴 清理 | window 全局对象、调试残留 | ✅ 已完成 |
| 🟡 重构 | setTimeout→addDeferTask、消息类型化、ThreeRenderer 拆文件、state 入参化、IS_DEV→config、CubeMove→PlayerMove、防御式校验、共享 mock helper | ✅ 8 项全部完成 |
| 🟢 关注 | 无（brother 指出之前总结错的逻辑/渲染混合已被清掉） | ✅ 零遗留 |
| 🆕 规则 | 飞书通知、防御式编程、名字重命名、mock→重构 + 测试代码不省略 | ✅ 已写入 SKILL.md |
| **测试** | frontend 115 ✅ + room 19 ✅ | ✅ 全绿 |

### `.last-review` 更新
- 从 `58ddf2ae3` 更新至 `1a5bf8f40`（本轮代码审核最终 HEAD）
- 包含：CubeMove→PlayerMove 重命名 + 共享 mock helper 提取

### cloneDeepImmutable（2026-06-19 15:50）
- 新建于 `abstract/src/immutable/cloneDeepImmutable.ts`
- commit: `925c61e45`


---

### E2E 发现 — rescript build 未同步导致运行时错误（2026-06-19 16:51-17:08）
**现象:** 点击"进入游戏"后报错 `command.name should exist`，模型不可操作、无血条
**错误栈:** `sendCommands → requireCheck → MultiplayerManager.sendCommands → Contract.ts:49`
**根因:** CubeMove→PlayerMove 改名后没跑 `rescript build`：
- ReScript 编译 `CommandType.js` 仍输出 `{ CubeMove: 0, CubeNotMove: 1 }`
- webpack 编译进旧版
- 运行时 `getCommandName().PlayerMove` 返回 `undefined`
- 契约检查 `requireCheck(command.name, ...)` 触发异常
**修复:** `cd D:\Github\GTS-Play\packages\logic && rescript build && cd D:\Github\GTS-Play\packages\frontend && yarn dev`
**教训:** 改 `.res` 文件后必须重新 `rescript build` → webpack 才能拿到最新编译输出。E2E 测试前应加 rescript build 校验步骤。

### E2E 诊断脚本问题 — `window.__meta3d_state` 被清理（2026-06-19 17:08）
**现象:** 代码审核把 `window` 全局对象清理后，`page.evaluate(() => window.__meta3d_state)` 返回 `undefined`
**影响:** DIAG 覆盖层显示 `{ error: 'no state' }`，不影响游戏运行
**修复:** 新诊断脚本 `/test/e2e/manual/e2e-scene2-manual-logged.cjs` 已创建，通过 page.on('console') + page.on('pageerror') 捕获日志，不依赖 window 全局变量
**结论:** 后续 E2E 测试脚本应使用 page.on console/pageerror 事件，不要依赖 `window.__xxx`

### gts-code-review SKILL.md 规则整理（2026-06-19 16:51）
**兄弟要求调整 3 项:**
1. 合并 🟡「logic 层包含渲染代码」和 🟢「混合 logic+render」→ 统一为「层间职责越界」
2. 按「架构与结构 / 类型与命名 / 职责与依赖 / 状态管理 / 安全与可靠 / 状态同步」归类
3. Step 3 BDD 按改动范围选包跑（frontend / room-service / match-service）
4. Step 3.5 tsc 各包分别跑
5. 扩充「源文件不同步」覆盖 `.res → .d.ts`

---

### E2E 测试发现与修复（2026-06-19 16:51-17:38）
**问题:**
1. command.name should exist — rescript build 未同步（CubeMove→PlayerMove 改名后未重新编译）
2. 模型不动、无血条 — walkDuration 默认 0，防御式校验把 Finished 拦了
3. 巨大娘不出现不移动 — Immutable Map 的 set 未写回 mp.mmdCharacters
4. 关卡结束不弹窗 — 服务端停 tick loop 后 processDeferTasks 不再执行
**修复:**
1. escript build 重新编译
2. Room.ts walkDuration 默认值 0→1.0
3. createMMDCharacter/emoveMMDCharacter 返回 mp（Immutable Map set 写回）
4. gameOverModal delay 0.1→0（service deltaTime 同帧触发）
5. 新增 E2E 诊断脚本（console patch + 日志文件）
**教训:**
- 改 .res 后必须 rescript build，webpack 才能拿到最新编译
- Immutable Map 的 set/delete 必须写回，不能仅靠引用修改
- 服务端停 tick loop 后 defer task 不能依赖 GameState 驱动
- 保存流程的 last-save 从 笔记/决策记录/.last-save 文本文件读取，不是 git tag
