# 2026-06-14 日记

## 碰撞盒系统（AABB） — 10:14~10:25
- GameState.res 新增 collisionBox 类型，playerState 加该字段
- ModelConfig.res: 归一化碰撞盒（@scale=1.0 基准），getCollisionBox 自动乘以 scale
- Movement.res: AABB 重叠检测（含自身碰撞体积），动画变化时更新碰撞盒
- 前端: IRenderer + ThreeRenderer + MultiplayerRender + MultiplayerLoop
- 服务端: PtlSetCollisionBox + ApiSetCollisionBox

## 碰撞盒重构（删除 SetCollisionBox） — 10:36~11:06
- 彻底删除 SetCollisionBox 流程，碰撞盒完全由服务端 ModelConfig 控制
- 删除 PtlSetCollisionBox.ts / ApiSetCollisionBox.ts / computeWorldBox / sendSetCollisionBox
- ModelConfig 巨大娘 box 恢复 base±1

## 通知系统改造（11:16）
- 弃用 cron（调度器死锁）/ Task Scheduler（弹窗）
- 方案定型：exec background → OpenClaw message send --channel feishu

## Config 重构（11:19~11:25）
- 新建 logic/config/Config.res，统一 Config.isTest
- Movement.res/CameraManager.res 删本地 isTest

## OBB 碰撞检测实现（14:00~14:54）
- 客户端上报 OBB（MMD 骨骼计算）→ 服务器 SAT 15 轴检测
- OBBComputer.ts 新建 + CommandManager/MultiplayerLoop/IRenderer 集成
- GIANTESS_OBB_DEFS 18 个部位 → 17 个（匹配 CollisionShapeData）

## OBB 测试补全（15:00~15:16）
- OBBComputer 8 BDD + Movement 3 OBB = Logic 13 + Frontend 64 ✅

## 速度减半（16:39~16:48）
- Movement.res speed 2.0→1.0，动画 deltaTime * 0.5
- OBB 硬编码重写匹配 CollisionShapeData，尺寸公式修复

## OBB 顶点自动生成（17:14~17:38）
- 放弃硬编码 GIANTESS_OBB_DEFS，改为从模型顶点数据 PCA 自动计算
- OBBAutoGen.ts: Jacobi 特征分解 + 顶点分组 + PCA OBB
- ComputeOBBAsset 类型 + computeOBBsFromAssets 运行时变换
- MultiplayerRender 加载时自动提取顶点
- Logic 40fps / Server IS_DEV 30fps（调优频率）

## PCA 中心偏移修复（17:38~17:43）
- OBB 整体偏上方 → PCA 中心在 mesh-local 空间，但 runtime 当 bone 局部偏移用了（double）
- 修正：存储 boneBindPos，runtime 用 offset = pcaCenter - boneBindPos
- 80/80 ✅

## OBB 偏移持续修复（17:46~17:58）
- 第一轮：`getWorldPosition` 传 plain object 抛异常 → 改用 `matrixWorld.elements[12-14]`
- 第二轮：PCA 在动画帧后读取，与骨骼位置不匹配 → 移到 `helper.add`/`helper.play` 之前
- 第三轮：mesh.scale=(5,5,5) 影响骨骼 matrixWorld → 临时 scale=(1,1,1) 再读
- 第四轮：双骨部位用所有骨骼平均位置（不只用 boneNames[0]）
- 80/80 ✅

## 相机限制解除（17:53~17:57）
- ThirdPersonCamera.ts + ThreeRenderer.ts 删 minDistance/maxDistance/maxPolarAngle

## OBB AutoGen 实战调优（18:08~18:27）
- PCA 左手系检测（axis3 翻转）
- 顶点分组阈值改后缀匹配（C/D/EX 副本骨骼）
- 问题：左足D/左足首D 等足部骨匹配了 左足 前缀，需要精确分层

## PCA 裁剪方案实现 + 测试 BDD（19:05~19:29）
- Torso trimPercentile，Shank/Thigh/Foot clipBones+clipPadding
- 全量 86/86 ✅

## OBB 参数调优 + 集成测试（19:34~20:01）
- GIANTESS_BONE_GROUPS 参数调整（Shank/Torso/TrigoneAndButt）
- BoneGroupDef 加 trimPercentile 属性
- PMX 二进制解析器 parsePmx.ts（不依赖 Three.js）
- 集成测试套件（jest.integration.json）+ fixture
- 110 全过 ✅

## SpatialBones 重构（20:21~20:47）
- OBBAutoGen.ts 全盘重写：空间范围分组（圆柱体/球体）替代蒙皮权重
- 去掉了 clipBones/clipPadding/trimPercentile
- 四肢自动半径检测（骨距×25%，最低0.6）
- 躯干自动肩宽检测 + 上半身2→下半身兜底
- GIANTESS_BONE_GROUPS：四肢用spatial，胸/头/臀保留 boneNames
- 测试重写：obb-clip→spatial 场景，全部 35/35 ✅

## RightThigh/LeftFoot/Torso 缺失问题（21:15~21:45）
- 用户测试发现部分 OBB 不生成 → 排查 fixture 是合成数据（非真实 PMX 解析）
- **决策：集成测试 fixture 必须使用真实 PMX 模型数据**
- 尝试用 parsePmx.ts 提取 → BaiXi 模型 header 有额外字节
- 修 parsePmx.ts（降级检测）→ 最终 39/39 全过 ✅
- **兄弟建议：用 MMDLoader 读取 PMX 顶点，不用自己写 parser**（正确方向，测试环境本来就有 Three.js 依赖）

### 改用 MMDParser + 修复问题
1. 用 Three.js `MMDParser`（`three/examples/jsm/libs/mmdparser.module.js`）生成真实 fixture
2. `test/utils/generateFixture.mjs`：一次性 ESM 脚本，提取全部 13338 真实顶点
3. baixi-obb.json 从 270 合成顶点 → **13338 真实顶点**（1.8MB）

### 真实 BaiXi 骨骼数据发现
- 上半身(Y=12.942)和下半身(Y=12.884) **距离仅 0.058** → Torso 圆柱退化
- 上半身2(Y=14.032)→下半身 距离 1.150 → 正确轴
- 左肩C/右肩C 在实际肩位置(±1.094)，而 左肩/右肩 在中心附近(±0.235)
- 脚部 432 顶点在踝趾圆柱内（前 5000 顶点只有头/上半身）
- 没有 D variant for torso/shoulder（上半身D/左肩D 都不存在）

### 修复内容
1. **Torso spatial**: `["上半身", "下半身"]` → `["上半身2", "下半身"]`
2. **Torso 半径检测**: `_detectTorsoRadius` 改取最大肩宽（后缀 C/D/EX/c/d/""，优先捕获 C 变体肩宽 2.187）
3. **Torso 分支**: `spatial.bones[0].startsWith("上半身")` 而非 === "上半身"
4. **Fixture 全量**: slice(0,5000) → 全部 13338

### 测试结果
- 集成 4/4 ✅ + obb 35/35 ✅ = **39/39 全过**
- Torso spatial OBB: center=(-0.00,13.46,0.48) half=(1.18,1.11,0.70) ✅
- Foot OBB 432 顶点 ✅, Shank OBB 289 顶点 ✅

### 关键决策
- **Fixture 用 MMDParser 一次性生成**：不可在 jest 中直接 import（ESM 兼容性问题）
- **`_detectTorsoRadius` 取最大肩宽**：迭代求 max，优先捕获 C 变体
- **`generateFixture.mjs` 保留为 fixture 根源**，PMX 更新时重新跑

## Torso 不包含手臂/臀部 + RightShank 对称确认（21:50~22:05）
### 修复内容
1. **Torso 去除 boneNames 兜底**：只用 spatial 圆柱体，避免包含 TrigoneAndButt
2. **Torso 加 radius: 0.95**：比肩半宽(1.094)窄，排除手臂顶点
3. **Torso 半宽 0.96 < 1.09** ✅，胸部 Z 分离 1.0 ✅
### 测试结果
- 集成 4/4 ✅ + OBB 35/35 ✅ = 67/67 前端全通过 ✅
