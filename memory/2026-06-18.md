# 2026-06-18

## E2E 智能导航
- 实现基于相机旋转 + 闭环跟踪的智能导航替换盲操 WASD
- 方案文档：`笔记/方案/2026-06-18-E2E智能导航方案.md`
- 首次测试：3.9 秒精准到达目标，HP 100→95 ✅

## 重构
- 封装 `e2e-helpers.cjs` 共享模块，4 个脚本净减少 125 行重复代码

## 基础设施
- 修复飞书通知：按 hostname 自动选择 bot（DESKTOP-HAOFHBA→新bot，其他→旧bot）
- `yarn install` 后 room/match 服务恢复正常

## 决策
- e2e 脚本统一通过 helpers 模块引用公共函数
- 飞书 bot 选择逻辑集中在 helpers + MEMORY.md 规则中
- 以后审核 e2e 改动时检查是否有可提取的重复代码
# 2026-06-18

## E2E 智能导航
- 实现基于相机旋转 + 闭环跟踪的智能导航替换盲操 WASD
- 方案文档：`笔记/方案/2026-06-18-E2E智能导航方案.md`
- 首次测试：3.9 秒精准到达目标，HP 100→95 ✅

## 重构
- 封装 `e2e-helpers.cjs` 共享模块，4 个脚本净减少 125 行重复代码

## 基础设施
- 修复飞书通知：按 hostname 自动选择 bot（DESKTOP-HAOFHBA→新bot，其他→旧bot）
- `yarn install` 后 room/match 服务恢复正常

## 代码审核 (2026-06-18)
- 审核范围：137 文件 (+5547/-462)
- 🔴1: `window.__threeRenderer` → 应改为 state 字段 `cameraAzimuth`，**不能挂 window** ⚠️（第一版错误地换了 `__e2e` 全局，待修正）
- 🟡2: IRenderer 补 `addSceneNode`/`removeSceneNode` → ✅ 补接口 + MultiplayerRender wrapper + MultiplayerHUD 删 `as any`
- 🟡3: `(p as any).hp` → ✅ 删掉（playerState 已有 hp/maxHp）
- 🟡4: MultiplayerHUD 模块级 Map 状态 → ✅ 迁移到 `multiplayer.hud` 统一管理
- 🟡5: 内联 HUD 逻辑 → ✅ 在 #4 中自然精简
- BDD: 11/11 HUD 测试通过
- 待修复：`window.__e2e` 仍需改为 state 字段

## 决策
- e2e 脚本统一通过 helpers 模块引用公共函数
- 飞书 bot 选择逻辑集中在 helpers + MEMORY.md 规则中
- 以后审核 e2e 改动时检查是否有可提取的重复代码
- **改 Skill 文件必须走 `skill_workshop`，不能直接 edit/write**
- **改 node_modules 前必须经兄弟确认**
- 保存前检查 1 小时内是否有代码审核（gts-save-flow Step 0）
- **重构规则：模块状态移到 state 中统一管理**
- 全局数据（camera azimuth）通过 state 暴露，不挂 window
