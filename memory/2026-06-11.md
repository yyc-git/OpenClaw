# 2026-06-11

## 飞书通知改造 & Token 优化
- 飞书通知从 `sessions_send` 切换为 **cron announce** 模式（一次性任务 + isolated + deleteAfterRun）
- 优点：不建持久会话、不积累上下文、token 消耗极低
- 实测三次均成功送达 ✅
- MEMORY.md 已更新通知方式
- 分析了缓存命中率：飞书通知不影响主会话 DeepSeek prefix cache
- 缓存命中率低的原因：thinking level 切换 / 多会话碎片化 / 上下文变化大
- 当前 context 37k/1M（4%），auto-compaction 已启用无需干预
- Context 优化策略：auto-compaction、子会话隔离、lightContext、跨 session 切分

## 多人联机小人模型 + 动画改造
- 参考单人 LittleMan 管线，用 FBXLoader + AnimationMixer + cross-fade
- 每个玩家独立 loadAsync FBX（不 clone），clip 重命名 idle/run
- 资源：从 demos 复制 Idle.fbx、Running.fbx

## ModelConfig 配置化（DefaultFacing）
- Infantry.fbx 默认朝向 -Z（BackwardZ），Cube 默认 +Z（ForwardZ）
- `computeFaceAngle(dx, dz, facing)` 统一补偿
- 后迁移至 logic/src/config/ModelConfig.res（纯 ReScript + genType 导出）

## animationName 同步
- playerState 加 animationName 字段，服务端广播
- 远程玩家动画用服务器推送的 animationName，不用本地推断

## 多人联机 Bug 修复历程

### 第一轮：旋转方向 + 动画同步（14:00~14:30）
- 本地移动时旋转没用 BackwardZ 补偿 → 修复用 computeFaceAngle
- 远程玩家 rotationY 没补偿 → 区分 idle/running 分别处理
- animationName 不在广播 schema → 加字段 + 重编译

### 第二轮：重构到服务端处理（14:30~14:50）
- Movement.res 统一处理：移动纯几何、空闲 +PI 补偿
- 客户端只渲染，零补偿逻辑

### 第三轮：ModelConfig 移到 logic/（14:50~15:05）
- logic/ 只有 .res 代码，没有 .ts
- 用 genType 导出类型给 TS 用

## 架构重构 Part 1：逻辑移到 logic/（17:00~18:14）

### 1a. CameraManager → logic/src/logic/CameraManager.res ✅
- 三个纯函数用 ReScript + genType 重写

### 1b. 命令数据改为客户端只传 keyState
- Movement.res handleCubeMove/handleCubeNotMove 用 CameraManager 算 worldMove/idleAngle
- Loop.ts 不再调 computeRelativeMovement/computeLookAngle

### 1c. renderFrame 去掉 keyState
- AnimationState 统一读服务端 p.animationName

## 架构重构 Part 2：IRenderer 原子接口 + ThreeRenderer 重写

### 新组件
- **IRenderer** 原子接口（opaque handles：SceneHandle/CameraHandle/FbxModelHandle/MixerHandle/ActionHandle）
- **ThreeRenderer** 重写（loadFbxModel / addEntity / createMixer / createAction / playAction / crossFade）
- **ManageScene** 模块（init/update/render/dispose，函数式，数据存 multiplayer state，不知道 Three.js）
- **Loop.initForMultiplayer** 统一初始化入口
- **MultiplayerHall** 简化到只调 Loop

### 修复的 Bug
1. `sendCommands.then(writeState)` 异步回调覆盖 GameState 更新 → 去掉 writeState
2. `clone(true)` 导致 SkinnedMesh 位置不动 → 改为每个玩家独立 load FBX
3. `mixer.clipAction` 同名 clip 冲突（都叫 "mixamo.com"）→ 创建时唯一名
4. idle/run action key 冲突（clipIndex 都是 0）→ 唯一 key（nextId）
5. 相机不跟随 → followTarget 直接设 camera position
6. 状态不同步 → 规范 writeState，state 只在开头 read 末尾 write

## 重要教训
1. **先写方案文档到项目 memory**，兄弟审核后再开干
2. **logic/ 只有 .res 代码**，genType 导出 .gen.tsx 给 TS
3. **客户端只渲染**，所有逻辑在服务端
4. **state 只在开头 readState，末尾 writeState**，中间参数传递
5. **发完飞书通知后不要回复飞书 bot**
6. **创建文档加头信息**（创建日期、作者、状态、概述）
7. **用户确认前 no_reply，不消耗 token**

## BDD 测试
- 最终 33/33 通过 ✅
- 服务端 8 场景（game.feature）
- 前端 manage-scene 5 场景（manage-scene.feature）
- 前端其他 20 场景（camera/render/helpers/multiplayer）
