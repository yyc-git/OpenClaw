
### 自动化 E2E 场景1最终结果（22:28-22:33）
三次自动化运行全部显示 WebSocket 未连接（`WS_NOT_OPEN`），但用户手动玩正常。
t1 始终能捕获到 ApiReq/ApiErr 日志，t2 因日志几乎全是 GameState（被过滤器跳过）而接近为空。
**根因猜测**：自动化流程中游戏启动到 WebSocket 建立有延迟，AllCommands 在 WS 就绪前就开始发送，导致大量错误。
**下次修复方向**：t2 的 `Get-Logs` 过滤不对（1615条全是 GameState），应考虑不过滤或单独处理。

### 记忆保存 & 自动关机（22:33）
按兄弟指令，场景1验收不通过后保存记忆，自动关机。
