# 2026-06-12 日志

## 多人联机状态重构（第二轮）
- 将 `initForMultiplayer` 从 `Loop.ts` 移到 `MultiplayerLoop.ts`（含 renderer.init + initOrbitControls）
- 模块变量全部消除：renderer/lastFrameTime/lastSendTime 存入 multiplayer state
- CommandManager 改为纯函数：processInputAndSendCommands(state, renderer) → state
- Loop.ts 精简为只 re-export loopForMultiplayer
- ManageScene.ts 不再调 CommandManager（输入处理在 loopForMultiplayer 层）
- MultiplayerHall.tsx 改导入路径 + 移除重复的 renderer.init/initOrbitControls
- 修复预存 tsc 错误：创建 HashMapType.gen.tsx
- **补了 8 个 BDD 测试**（CommandManager: shouldSend/buildCommand/processInputAndSendCommands/resetSendTimer）

## Loader 模块提取
- 新增 `src/logic_layer/Loader.ts` — 资源加载器：fetch ArrayBuffer + 重试3次 + 退避延迟
- 新增 `src/logic_layer/StorageCache.ts` — localforage 离线缓存封装，含版本管理
- `IRenderer.ts` 新增 `loadFbxModelFromBuffer(path, buffer)`
- `ThreeRenderer.ts` 实现 `loadFbxModelFromBuffer`（FBXLoader.parse）
- `ManageScene.ts` init 改用 Loader.loadFbx + renderer.loadFbxModelFromBuffer
- 新增 `test/features/loader.feature` + `test/step-definitions/loader.steps.ts`
- **补了 4 个 BDD 测试**（fetchWithRetry成功/重试成功 / loadFbx缓存未命中/缓存命中）
- 全量 45 测试全部通过

## 前端启动
- 正确的启动命令：`yarn webpack:dev-server`，对应 `webpack.config.devserver.js`
- 之前用了 `webpack.config.js` 导致编译失败（MMD 文件名冲突）

## 精简化（两轮）
- 第一轮：MEMORY.md 7208→3734, AGENTS.md 4263→669, SOUL.md 1806→546, TOOLS.md 1129→126（15.5k→5k，-68%）
- 第二轮：MEMORY.md 3734→1705, AGENTS.md 669→593, HEARTBEAT.md 429→175（6.1k→3.8k，-38%）
- 总累计：15.5k→3.8k，-75%

## QMD 记忆系统
- 修复：memorySearch.provider 设为 "none" 避免 OpenAI API key 要求
- 工作区索引：QMD collection 创建，23 文件 + 71 分片嵌入（34s，GPU）
- GTS-Play 项目文档索引：5 文件 + 34 分片嵌入（12s）
- 会话索引：68 会话文件通过 BM25 索引（跳过向量嵌入，太大超时）
- 搜索模式：search（BM25），1.2s 响应
- SQLite 锁问题：Windows 上 OpenClaw QMD manager 和 search 争锁，实际走内置 fallback（1.5s 出结果）
- 尝试 query 模式但本地 model 太慢→回退 search
- 尝试 sessions.enabled=true 但 embed 超时→取消向量嵌入只做 BM25

## 其他
- 深潶 API 缓存优化：对话助手场景命中率 2% 正常，没必要折腾
- compaction：reserveTokens=800000（20%自动压缩）
- Git remote：HTTPS→SSH（GFW 封锁 443）
- 所有改动在当前会话中完成，不开子会话
- 规则强化：完成任何任务必须发飞书通知（<10 字）
- Token 优化协议：exec 管道收敛（tail/Select-String/2>$null）+ 读文件切段（offset/limit），记录到 MEMORY.md
