# 2026-06-16 日志

## 今日工作

### Obsidian 笔记库重构
- 删除了 `D:\Github\GTS-Play\memory/` 目录（全部文件已清空，保留空目录权限问题未完全删除）
- 将 `笔记/方案/多人联机LittleMan加载与动画实施方案.md` 从摘要版更新为完整版（memory 原版）
- 更新 `笔记/索引.md` — 移除 memory/ 引用
- 更新项目文档日期（`CONSOLIDATED-MEMORY.md`、`GTS-Play-Project-Index.md` → 2026-06-16）
- 更新 `MEMORY.md`：
  - 保存协议第4步改为写 `笔记/项目文档/` 而非 `memory/`
  - 新增「Obsidian 笔记库」章节，定义各目录用途及保存流程写入规则
- 清理 `MEMORY.md` 中关于 memory/ 项目索引的旧引用

### 待办
- E2E 自动化场景1（WS_NOT_OPEN）待修复
# 2026-06-16 日志

## 今日工作

### Obsidian 笔记库重构
- 删除了 `D:\Github\GTS-Play\memory/` 目录（全部文件已清空，保留空目录权限问题未完全删除）
- 将 `笔记/方案/多人联机LittleMan加载与动画实施方案.md` 从摘要版更新为完整版（memory 原版）
- 更新 `笔记/索引.md` — 移除 memory/ 引用
- 更新项目文档日期（`CONSOLIDATED-MEMORY.md`、`GTS-Play-Project-Index.md` → 2026-06-16）
- 更新 `MEMORY.md`：
  - 保存协议第4步改为写 `笔记/项目文档/` 而非 `memory/`
  - 新增「Obsidian 笔记库」章节，定义各目录用途及保存流程写入规则
- 清理 `MEMORY.md` 中关于 memory/ 项目索引的旧引用

### 开机自启 Gateway
- 检测到 `OpenClaw Gateway` 计划任务已禁用，重新注册并启用为开机自启
- 登录 Windows 后 Gateway 自动启动

### E2E 验收 — WS 断连修复（核心）
- 场景1 三次验收未通过，根因：`initForMultiplayer` 加载 FBX 期间心跳超时（5s）导致 WS 断开
- **修复 `MultiplayerManager.ts`**：
  - `connect()` 心跳超时 5s → 15s
  - 加 `_waitWsReady` 辅助函数轮询确认底层 `isConnected=true`
- **修复 `MultiplayerLoop.ts`**：
  - `loopForMultiplayer` 入口加 `mp.isConnected` 守卫，未连接时跳过命令发送
- 场景1 两次 Playwright E2E 稳定通过 ✅（error=0, WS_NOT_OPEN=0）
- 优化版从 55s 提速到 29s（waitForSelector 替代固定 sleep）

### E2E 脚本体系建立
- 统一使用 Playwright（独立浏览器），废弃 OpenClaw browser CLI（MCP）方案
- 三个场景脚本：
  - `test/e2e/e2e-pw.cjs` — 场景1（全自动验收）
  - `test/e2e/e2e-scenario2.cjs` — 场景2（手动玩 + 日志录制）
  - `test/e2e/e2e-scenario3.cjs` — 场景3（手动玩 + 性能录制）
- 浮层（右上角半透明 overlay）替代 flag 文件 / 聊天命令来控制开始/停止
- 双窗口并排显示（左 player1 右 player2，各 680×750）
- 日志保存到 `test/e2e/monitor/`，分析后问是否删除

### 场景3 性能录制
- 使用 CDP Profiler（Chrome DevTools Protocol）录制完整 CPU Profile
- 分析 >1ms 的所有函数调用，按总耗时排序
- 浮层按钮控制：点击「▶ 开始录制」启动，点击「⏹ 停止录制」停止
- 2 分钟超时自动停止
- 录制内容：CPU Profile + FPS + API 耗时 + GameState 间隔
- 输出摘要 + 优化建议，自动删除原始数据

## 新增记忆

### 验收规则（硬性规定）
- 兄弟设置验收标准后，默认必须通过，不通过则一直修改直到通过
- 默认总超时 1 小时
- 每次改完自动跑测试，不通过继续修

### 通知协议
- 验收通过或中途终止都要发飞书通知
- 内容要详细：结果、关键指标、改动要点

### 日志保留协议
- 场景1、2、3 运行后先保留日志
- 分析后给总结，再问兄弟是否删除

### Playwright E2E 方案
- 场景1：全自动（两个独立 context 登录→创建→加入→等 canvas→验证→退出）
- 场景2：手动玩（双窗口并排 + 浮层停止按钮 → 抓日志 → 分析）
- 场景3：性能录制（CDP Profiler + FPS + API 计时 → 摘要 + 优化建议）
- 浮层设计：右上角半透明，pointer-events:auto，含可点击按钮
- 双窗口并排：两个独立 Chrome 窗口，各 680×750，左右对齐不重叠

### 已知问题
- Playwright 方案比 OpenClaw browser CLI 稳定且快，但需要保持服务运行
- 启动顺序：room-service(4003) → match-service(3000) → frontend(8093)

### 场景1 E2E 自动测试（10:26-10:41，会话 ba0c6e11）

兄弟要求跑场景1全自动测试：自动登录 → 创建/加入房间 → 自动移动双角色 → 抓日志 → 验收双方有移动记录 → 不通过自动修复（1小时超时）

**第1轮（briny-shell）：** 代码 bug — `setupConsoleCapture` 返回对象 {logs:[]} 但代码当数组用 → `logs1.join is not a function`
**修复：** 加 `.logs`

**第2轮（quick-claw）：** 未能进游戏 — 服务端脏数据，跑了两个 webpack 实例
**修复：** 重启 webpack-dev-server

**第3轮（tidal-shell）：** ✅ 能进游戏 + 自动移动，但日志分析报 `Cannot read properties of undefined (reading 'join')`
- 两个角色成功进入游戏、播放行走动画 ✅
- 根因：`logs1` 在重试轮变为 undefined，page.on 监听器在页面关闭后导致 `setupConsoleCapture` 赋值失败
**修复：** 加 `if (!logs1 || !logs2) { continue }` 守卫

**最终：** 会话被 abort，遗留3个孤儿 Node 进程（10:27/10:34/10:37 各启动一个），均已被清理
- 代码已修但未验收通过 — 第4轮（已修复版）跑了一半会话终止
- 36个场景2日志文件（各200KB）待清理
